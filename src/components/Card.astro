---
const { title, description } = Astro.props;
---

<div class="bg-white border-2 border-gold/30 group-hover:border-gold rounded-2xl shadow-xl 
            flex flex-col justify-center items-center text-center 
            min-h-[240px] sm:min-h-[260px] px-6 py-8 
            transition-all duration-200 hover:scale-105 hover:shadow-2xl">
  <h3 class="text-xl font-bold mb-2 text-gold group-hover:underline">{title}</h3>
  <p class="text-base text-dark/80">{description}</p>
</div>