---
import Layout from '../../layouts/DefaultLayout.astro';

const locations = [
  {
    slug: 'stein-am-rhein',
    name: 'Stein am Rhein',
    teaser: 'IT-Beratung, Support und Digitalisierung direkt vor Ort – persönlich, unkompliziert, schnell.',
    services: ['Vor-Ort-IT-Support', 'Digitalisierung & Automatisierung', 'Workshops & Schulungen'],
  },
  {
    slug: 'schaffhausen',
    name: 'Kanton Schaffhausen',
    teaser: 'Ganzheitliche IT-Betreuung für KMU, Selbständige und Private im ganzen Kanton.',
    services: ['Managed IT-Services', 'Cybersecurity & Backups', 'Cloud & Collaboration'],
  },
];
---

<Layout
  title="IT-Service Standorte – Stein am Rhein & Schaffhausen | JH Virtuell"
  description="IT-Beratung, Support & Digitalisierung vor Ort: Stein am Rhein, Schaffhausen und ganze Schweiz. Finde den passenden Standort und lerne unsere Leistungen kennen.">

  <section class="relative bg-gradient-to-b from-gold/10 via-light to-white pt-10 md:pt-14 pb-20 px-4 min-h-screen">
    <div class="max-w-4xl mx-auto text-center mb-12">
      <h1 class="text-4xl sm:text-5xl font-extrabold text-gold tracking-tight drop-shadow">IT vor Ort – unsere Standorte</h1>
      <p class="text-lg sm:text-xl text-dark/80 mt-4">
        Wir unterstützen Unternehmen, Selbständige und Privatkund:innen in Stein am Rhein, im Kanton Schaffhausen und schweizweit remote.
        Wähle den Standort, der am besten zu dir passt – das erste Gespräch ist kostenlos.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-5xl mx-auto">
      {locations.map((location) => (
        <article class="bg-white border border-gold/20 rounded-2xl shadow-lg p-8 flex flex-col justify-between hover:shadow-2xl hover:border-gold/60 transition">
          <div>
            <h2 class="text-2xl font-bold text-gold mb-2">{location.name}</h2>
            <p class="text-dark/80 mb-5">{location.teaser}</p>
            <ul class="space-y-2 text-dark/70 mb-6">
              {location.services.map(item => (
                <li class="flex items-start gap-2">
                  <span aria-hidden="true" class="text-gold">•</span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>
          <a
            href={`/standorte/${location.slug}`}
            class="inline-block bg-gold text-dark font-semibold px-6 py-3 rounded-xl shadow hover:opacity-90 transition text-center"
          >
            Zum Standort {location.name}
          </a>
        </article>
      ))}
    </div>

    <div class="max-w-4xl mx-auto mt-16 text-center">
      <p class="text-dark/70 text-base leading-relaxed">
        Kein passender Ort dabei? Unsere Projekte laufen schweizweit remote – mit sicherem Zugriff, klaren Prozessen und persönlichem Kontakt.
        <a href="/kontakt" class="text-gold font-semibold hover:underline">Melde dich für ein Gespräch</a> und wir finden die passende Lösung.
      </p>
    </div>
  </section>
</Layout>
