---
import Layout from '../../layouts/DefaultLayout.astro';
import { withVersion } from '../../utils/version.ts';
---

<Layout
  title="Alter berechnen – Jahre, Monate & Tage ab Geburtsdatum | JH Virtuell"
  description="Wie alt bist du genau? Berechne dein Alter präzise in Jahren, Monaten und Tagen – und als Gesamtzahl in Tagen, Stunden & Sekunden. Kostenlos, anonym und ohne Werbung.">

  <!-- Stabil: nutzt globalen Header-Spacer; kein Weissspalt -->
  <section class="relative bg-gradient-to-b from-gold/10 via-light to-white pt-8 md:pt-12 lg:pt-16 pb-20 px-4 min-h-screen text-center">

    <!-- (entfernt) Deko-Bogen oben – verursachte Weisskante auf Ultra-Wide -->

    <div class="max-w-3xl mx-auto relative z-10">
      <h1 class="text-4xl sm:text-5xl font-extrabold mb-3 text-gold tracking-tight drop-shadow">
        Alter berechnen
      </h1>
      <h2 class="text-lg sm:text-xl font-semibold text-dark/70 mb-3">
        Jahre, Monate, Tage – plus Gesamt-Tage, Stunden & Sekunden
      </h2>
      <p class="text-lg sm:text-xl text-dark/80 mb-10">
        Gib dein Geburtsdatum ein – wir zeigen dir dein exaktes Alter in klarer Aufschlüsselung. Läuft lokal im Browser, ohne Speicherung.
      </p>

      <form
        id="ageForm"
        class="bg-white p-6 sm:p-8 rounded-2xl shadow-xl border-2 border-gold space-y-6 max-w-md mx-auto"
        aria-label="Alter berechnen"
      >
        <div>
          <label for="birthdate" class="block mb-1 font-semibold text-dark">Geburtsdatum</label>
          <input
            type="date"
            id="birthdate"
            name="birthdate"
            required
            aria-required="true"
            class="w-full border border-gray-300 rounded-lg px-4 py-2"
          />
        </div>
        <button type="submit" class="bg-[#DCB957] hover:bg-[#c9a84d] text-white font-semibold py-2 px-6 rounded-xl transition">
          Alter berechnen
        </button>
        <div id="ageResult" class="mt-6 text-lg font-semibold text-dark space-y-2 text-left sm:text-center" role="status"></div>
      </form>

      <p class="text-sm text-dark/60 mt-10 max-w-lg mx-auto">
        Dein Alter wird lokal in deinem Browser berechnet – 100 % datenschutzfreundlich & anonym.
      </p>

      <!-- Unterstützungs-Box -->
      <div class="mt-20 max-w-xl mx-auto text-center text-dark/70 text-sm sm:text-base">
        <p class="mb-3">🎁 Du nutzt meine Tools gerne und möchtest mir einen kleinen Kaffee spendieren?</p>
        <p class="mb-4">Dann freue ich mich über ein <strong>Trinkgeld von 2 CHF via PayPal</strong> 💛</p>
        <a href="https://www.paypal.me/jhvirtuell/2" target="_blank" rel="noopener" class="inline-block bg-gold hover:bg-[#c9a84d] text-white font-semibold px-6 py-2 rounded-xl shadow transition">
          Jetzt 2 CHF spenden
        </a>
        <p class="text-xs text-dark/50 mt-3">Zahlung über <code>info@jh-virtuell.ch</code> via PayPal</p>
      </div>
    </div>

    <!-- (behalten) Deko-Bogen unten – unter Content, kein Weissspalt -->
    <div class="absolute bottom-0 left-0 w-full pointer-events-none z-0" aria-hidden="true">
      <svg viewBox="0 0 1440 70"><path fill="#fff" d="M0,10Q720,70 1440,10L1440,70L0,70Z" /></svg>
    </div>

    <!-- JS mit Cache-Busting -->
    <script src={withVersion('/scripts/alter-berechnen.js')} defer></script>

    <!-- Strukturierte Daten -->
    <script type="application/ld+json" is:inline>
      {JSON.stringify({
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "Alter berechnen",
        "applicationCategory": "CalculatorApplication",
        "operatingSystem": "All",
        "description": "Berechne dein Alter exakt in Jahren, Monaten, Tagen – und als Gesamt-Tage, Stunden und Sekunden. Lokal & anonym.",
        "url": "https://jh-virtuell.ch/tools/alter-berechnen",
        "offers": { "@type": "Offer", "price": "0", "priceCurrency": "CHF" }
      })}
    </script>
  </section>
</Layout>