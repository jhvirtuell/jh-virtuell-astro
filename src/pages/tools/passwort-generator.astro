---
import Layout from '../../layouts/DefaultLayout.astro';

const title = 'Passwort Generator – Sicheres Passwort erstellen | JH Virtuell Tools';
const description = 'Erstelle jetzt ein sicheres Passwort mit unserem kostenlosen Passwort-Generator. Stark, zufällig und DSGVO-konform – ideal für Logins, Konten und mehr.';
---

<Layout title={title} description={description}>

  <!-- Stabil: nutzt globalen Header-Spacer; kein Weissspalt -->
  <section class="relative bg-gradient-to-b from-gold/10 via-light to-white pt-8 md:pt-12 lg:pt-16 pb-20 px-4 min-h-screen">

    <!-- (entfernt) Top Curve: verursachte Weisskante auf Ultra-Wide -->

    <div class="max-w-2xl mx-auto text-center relative z-10">
      <h1 class="text-4xl sm:text-5xl font-extrabold mb-3 text-gold tracking-tight drop-shadow">
        Sicheres Passwort erstellen
      </h1>
      <h2 class="text-lg sm:text-xl font-semibold text-dark/70 mb-3">
        Schnell, anonym & werbefrei – direkt im Browser
      </h2>
      <p class="text-lg sm:text-xl text-dark/80 mb-10">
        Unser kostenloser Passwort-Generator erstellt starke, zufällige Passwörter für maximale Sicherheit. Ohne Tracking. Ohne Werbung.
      </p>

      <!-- Generator Box -->
      <div class="bg-white rounded-2xl shadow-xl p-10 mb-10 border-2 border-gold text-left">
        <label class="block text-dark font-medium mb-2" for="length">Länge des Passworts:</label>
        <input id="length" type="number" min="8" max="64" value="16" class="w-full p-2 border border-gray-300 rounded mb-4" />

        <label class="block text-dark font-medium mb-2">Optionen:</label>
        <div class="flex flex-col sm:flex-row gap-4 mb-4">
          <label class="cursor-pointer"><input type="checkbox" id="lowercase" checked /> Kleinbuchstaben</label>
          <label class="cursor-pointer"><input type="checkbox" id="uppercase" checked /> Grossbuchstaben</label>
          <label class="cursor-pointer"><input type="checkbox" id="numbers" checked /> Zahlen</label>
          <label class="cursor-pointer"><input type="checkbox" id="symbols" /> Sonderzeichen</label>
        </div>

        <button onclick="generatePassword()" class="bg-gold text-white px-6 py-2 rounded font-semibold shadow hover:opacity-90 transition">
          Passwort generieren
        </button>

        <div class="mt-6">
          <label class="block text-dark font-medium mb-2" for="result">Dein Passwort:</label>
          <div class="flex gap-2">
            <input id="result" type="text" readonly class="w-full p-3 border border-gray-300 rounded font-mono text-lg bg-gray-50" />
            <button onclick="copyToClipboard()" title="In Zwischenablage kopieren" class="px-4 py-2 bg-gray-200 hover:bg-gray-300 rounded font-semibold">📋</button>
          </div>
          <p id="copy-notice" class="text-green-600 text-sm mt-2 hidden">
            ✅ Passwort wurde in die Zwischenablage kopiert!
          </p>
        </div>
      </div>

      <p class="text-base text-dark/80 max-w-xl mx-auto">
        Verwende dieses Tool, um sichere Passwörter für E-Mails, Konten, Logins oder WLAN-Zugänge zu erstellen.
        Halte dein Passwort stets geheim und speichere es sicher – z. B. in einem Passwort-Manager.
      </p>
    </div>

    <!-- Unterstützungs-Box -->
    <div class="mt-20 max-w-xl mx-auto text-center text-dark/70 text-sm sm:text-base">
      <p class="mb-3">🎁 Du nutzt meine Tools gerne und möchtest mir einen kleinen Kaffee spendieren?</p>
      <p class="mb-4">Dann freue ich mich über ein <strong>Trinkgeld von 2 CHF via PayPal</strong> 💛</p>
      <a href="https://www.paypal.me/jhvirtuell/2" target="_blank" rel="noopener" class="inline-block bg-gold hover:bg-[#c9a84d] text-white font-semibold px-6 py-2 rounded-xl shadow transition">
        Jetzt 2 CHF spenden
      </a>
      <p class="text-xs text-dark/50 mt-3">Zahlung über <code>info@jh-virtuell.ch</code> via PayPal</p>
    </div>

    <!-- Bottom Curve (unter Content, kein Weissspalt) -->
    <div class="absolute bottom-0 left-0 w-full pointer-events-none z-0" aria-hidden="true">
      <svg viewBox="0 0 1440 70" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill="#fff" d="M0,10Q720,70 1440,10L1440,70L0,70Z" /></svg>
    </div>

    <!-- JSON-LD: SoftwareApplication -->
    <script type="application/ld+json" is:inline>
      {JSON.stringify({
        "@context":"https://schema.org",
        "@type":"SoftwareApplication",
        "name":"Passwort Generator",
        "applicationCategory":"Utility",
        "operatingSystem":"All",
        "url":"https://jh-virtuell.ch/tools/passwort-generator",
        "description":"Sicheres Passwort online erstellen – zufällig, DSGVO-konform, ohne Tracking.",
        "offers":{"@type":"Offer","price":"0","priceCurrency":"CHF"}
      })}
    </script>

    <!-- JS-Import -->
    <script type="module">
      import { generatePassword, copyToClipboard } from '/scripts/password-generator.js';
      window.generatePassword = generatePassword;
      window.copyToClipboard = copyToClipboard;
    </script>

  </section>

</Layout>