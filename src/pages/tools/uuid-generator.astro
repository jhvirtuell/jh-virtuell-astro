---
import Layout from '../../layouts/DefaultLayout.astro';
import { withVersion } from '../../utils/version.ts';
---

<Layout
  title="UUID Generator – sichere UUIDv4 online erzeugen | JH Virtuell"
  description="Erzeuge zufällige UUIDv4-IDs online – kostenlos, sicher und ohne Speicherung. Ideal für Entwickler, Projekte & Tools.">

  <!-- Stabil: nutzt globalen Header-Spacer; kein Weissspalt -->
  <section class="relative bg-gradient-to-b from-gold/10 via-light to-white pt-8 md:pt-12 lg:pt-16 pb-20 px-4 min-h-screen text-center">

    <!-- (entfernt) Deko-Bogen oben – verursachte Weisskante -->

    <div class="max-w-xl mx-auto relative z-10">
      <h1 class="text-4xl sm:text-5xl font-extrabold mb-3 text-gold tracking-tight drop-shadow">
        UUID Generator
      </h1>
      <h2 class="text-lg sm:text-xl font-semibold text-dark/70 mb-3">
        Schnell, anonym & werbefrei – direkt im Browser
      </h2>
      <p class="text-lg sm:text-xl text-dark/80 mb-10">
        Erzeuge schnell & sicher eine zufällige UUIDv4 – ideal für Entwickler, Formulare oder Datenbankeinträge.
      </p>

      <div class="bg-white p-10 rounded-2xl shadow-xl border-2 border-gold inline-block w-full">
        <div id="uuidOutput" class="text-lg sm:text-xl font-mono font-semibold text-dark break-words mb-4" aria-live="polite">
          Lade...
        </div>

        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <button id="generateUUID" class="bg-[#DCB957] hover:bg-[#c9a84d] text-white font-semibold py-2 px-6 rounded-xl transition">
            Neue UUID erzeugen
          </button>

          <button id="copyUUID" class="border border-gold text-gold hover:bg-gold/10 font-semibold py-2 px-6 rounded-xl transition" aria-label="UUID in die Zwischenablage kopieren">
            📋 Kopieren
          </button>
        </div>

        <div id="copyFeedback" class="mt-3 text-sm text-green-600 hidden">✅ UUID kopiert!</div>
      </div>

      <p class="text-sm text-dark/60 mt-10 max-w-lg mx-auto">
        Die erzeugte UUID basiert auf dem Zufallsverfahren v4 (RFC 4122) – kein Tracking, keine Speicherung.
      </p>
    </div>

    <!-- Info-Box zu UUID -->
    <div class="bg-white border-l-4 border-gold px-6 py-5 mt-16 text-left rounded-xl shadow-md max-w-2xl mx-auto text-dark/90 text-sm sm:text-base">
      <h3 class="text-lg font-bold mb-2 text-gold">Was ist eine UUID?</h3>
      <p class="mb-2">Eine UUID (Universally Unique Identifier) ist eine weltweit eindeutige ID, oft in der Softwareentwicklung genutzt – z. B. für Datenbankeinträge, Sitzungen oder API-Zugriffe.</p>
      <p class="mb-2">Die hier erzeugte UUID basiert auf <strong>UUIDv4</strong>, wird zufällig generiert und erlaubt keine Rückschlüsse auf Zeit oder Quelle.</p>
      <p>Die Wahrscheinlichkeit von Kollisionen ist praktisch null – ideal für technische Anwendungen.</p>
    </div>

    <!-- Unterstützungs-Box -->
    <div class="mt-20 max-w-xl mx-auto text-center text-dark/70 text-sm sm:text-base">
      <p class="mb-3">🎁 Du nutzt meine Tools gerne und möchtest mir einen kleinen Kaffee spendieren?</p>
      <p class="mb-4">Dann freue ich mich über ein <strong>Trinkgeld von 2 CHF via PayPal</strong> 💛</p>
      <a href="https://www.paypal.me/jhvirtuell/2" target="_blank" rel="noopener" class="inline-block bg-gold hover:bg-[#c9a84d] text-white font-semibold px-6 py-2 rounded-xl shadow transition">
        Jetzt 2 CHF spenden
      </a>
      <p class="text-xs text-dark/50 mt-3">Zahlung über <code>info@jh-virtuell.ch</code> via PayPal</p>
    </div>

    <!-- (behalten) Deko-Bogen unten – unter Content, kein Weissspalt -->
    <div class="absolute bottom-0 left-0 w-full pointer-events-none z-0" aria-hidden="true">
      <svg viewBox="0 0 1440 70" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path fill="#fff" d="M0,10Q720,70 1440,10L1440,70L0,70Z" />
      </svg>
    </div>

    <!-- JSON-LD: SoftwareApplication -->
    <script type="application/ld+json" is:inline>
      {JSON.stringify({
        "@context":"https://schema.org",
        "@type":"SoftwareApplication",
        "name":"UUID v4 Generator",
        "applicationCategory":"DeveloperApplication",
        "operatingSystem":"All",
        "url":"https://jh-virtuell.ch/tools/uuid-generator",
        "description":"UUID v4 online erstellen – kostenlos, sicher & ohne Speicherung. Ideal für Entwickler & Projekte.",
        "offers":{"@type":"Offer","price":"0","priceCurrency":"CHF"}
      })}
    </script>

    <!-- JS mit Cache-Busting -->
    <script src={withVersion('/scripts/uuid-generator.js')} defer></script>
  </section>
</Layout>